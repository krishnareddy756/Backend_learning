<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Tweets</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>All Tweets</h1>
    <% for (post of posts) { %>
        <div class="post">
            <h3 class="user">@<%= post.username %></h3>
            <h4><%= post.content %></h4>
            <br>
            <br>
            <a href="http://localhost:8080/posts/<%= post.id %>">View Tweet</a> 
            <br>
            <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit Tweet</a> 
            <br>
            <form method="post" action="http://localhost:8080/posts/<%= post.id %>?_method=DELETE">
                <button>Delete</button>
            </form>
            <!-- ✅ Absolute URL -->
        </div>
    <% } %>
    <a href="http://localhost:8080/posts/new">New Tweet</a>  <!-- ✅ Absolute URL -->
</body>
</html>
